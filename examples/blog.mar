(define-entity post
    (fields ((title string) (body string)))
    (belongs-to ((author user)))
    (defaults ((author current-user))))

(define-screen posts
  (view
    (section
      (title "Posts")
      (text "Blog tracks posts with authors linked to the current user."))))

(define-app blog
  (backend (entities post))
  (frontend (screens posts)))
